import styled from 'styled-components'

export const ImageInstance = styled.div`
  aspect-ratio: 5 / 4;
  border: var(--border-1);
  display: flex;
  justify-content: center;
  align-items: center;
  overflow: hidden;
  max-width: 30vw;
`
